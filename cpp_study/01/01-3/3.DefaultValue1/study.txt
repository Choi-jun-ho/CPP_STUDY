매개변수의 디폴트 값

디폴드 값 : 기본적으로 설정되어 있는 값

int MyFunc1 (int num = 7){ // 디폴드 값 함수호출시 인자를 전달하지 않으면 7이 전달된걸로 간주
	return num+1 
}

void MyFunc2 (int num1 = 1, int num2 = 2){...}

MyFunc2(3) -> num1에 3, num2에 2(디폴드값) 데이터 저장

전달되는 인자는 왼쪽에서 부터 채워지고 부족한것은 디폴드 값으로 채워진다